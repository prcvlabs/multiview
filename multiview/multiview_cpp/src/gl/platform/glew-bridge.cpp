
#include "glew-bridge.hpp"

#ifdef USE_QT_GL_FUNCTIONS

// #define CALL(f, ...) return ::f(__VA_ARGS__)

namespace perceive
{
// GLuint glCreateShader(GLenum type) { CALL(glCreateShader, type); }
// void glDeleteShader(GLuint shader) { CALL(glDeleteShader, shader); }
// void glDetachShader(GLuint p, GLuint s) { CALL(glDetachShader, p, s); }
// void glCompileShader(GLuint shader){ CALL(glCompileShader, shader); }
// void glGetShaderiv(GLuint s, GLenum p, GLint* q){CALL(glGetShaderiv,s,p,q);}
// void glGetShaderInfoLog(GLuint s, GLsizei b, GLsizei* l, char* i)
// { CALL(glGetShaderInfoLog, s,b,l,i); }

} // namespace perceive

// USE_QT_GL_FUNCTIONS
#endif
